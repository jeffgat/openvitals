# Blocked Requests

Track work that is blocked because it needs human input, device access, labels, captures, credentials, account actions, or product decisions. Keep engineering-only blockers in the relevant issue, TODO, or code comments instead.

## Active

### Device-Side Historical Cursor Sync

Status: blocked on owned command 33 payload evidence.

- 2026-06-14 Rust historical sync dry-run now models an explicit device cursor request and blocks it with `device_cursor_command_33_payload_unvalidated` unless a validated `set_read_pointer` command 33 payload is supplied.
- Current evidence confirms the normal flow as range poll, historical transfer request, packet consumption, and result ack; it does not prove how to convert "send only after timestamp X" into a safe device-side read pointer write.
- Human/device action needed: capture and label a known-good device-side read-pointer flow that includes the command 33 payload bytes, target timestamp or page semantics, command response, and the resulting first historical packets.
- Current fallback: continue full/range-first historical transfer with stable local packet identity dedupe and local checkpoint advancement from accepted historical packets only.

### Desktop Band Custom Notify

Status: blocked on Mac pairing/bonding.

- 2026-06-13 desktop debugger and native CoreBluetooth auth probes can scan and connect to the compatible band, discover `fd4b0002/0003/0004/0005/0007`, and send the `GET_HELLO` bytes, but custom fd4b notifications remain at 0.
- Native CoreBluetooth reports `Encryption is insufficient` for `fd4b0003`, `Authentication is insufficient` for `fd4b0004/0005/0007`, and `Authentication is insufficient` for the `fd4b0002` write. This confirms the blocker is the Mac link security state, not Rust parsing or command frame construction.
- 2026-06-13 follow-up: the band advertises the custom `fd4b0001` service at close range, but macOS Bluetooth Settings does not consistently expose it as a pairable/registerable accessory. A native CoreBluetooth secure-char probe does not trigger an OS pairing prompt.
- Human action needed: find a macOS-visible pairing/onboarding mode or private onboarding command path for the band, or obtain an already-bonded Mac link. Computer Use should ask for action-time confirmation before clicking any Bluetooth Settings row or control that may pair/bond a device.
- Current fallback: keep using the debugger's `Native Auth Probe` and `Start Band Parity Probe` diagnostics; do not treat command-write rows or HR strap `2A37` packets as successful band-side custom notify capture.

### Trusted Sleep And HRV Evidence

Status: partly unblocked for sleep v0, still blocked for HRV/recovery evidence.

- 2026-06-11 manual local bundle contains enough trusted motion/window evidence to compute `open_vitals.sleep.v0`; keep using sleep v0 until sleep v1 release-gate semantics are validated.
- 2026-06-11 K20 raw/research candidates are not enough to unblock HRV: direct i16 and peak-spacing interpretations failed HR-alignment validation against trusted HR in the aggregate diagnostic capture.
- 2026-06-11 K20 simple field-correlation pass did not reveal a direct beat-interval field either: strongest simple fields were moderate (~0.58 absolute correlation), and the most plausible 300-2000 raw range correlated in the wrong direction for RR.
- 2026-06-11 realtime raw export shows `REALTIME_RAW_DATA` is already decoded as K20/K21. K20 exposes structured 25-sample channel blocks, but K20/K26 validation remains below promotion threshold: direct i16 plausible intervals were 327/1,433 within 8 bpm and peak spacing was 247/1,121 within 8 bpm against trusted HR. The dedicated K20 channel scan found 1,832 K20 frames and 1,361 realtime K20 frames, but the best channel was only 3/5 usable HR-matched segments with 33% inside 8 bpm and ~14 bpm mean absolute error. Channel peak scans need a stronger transform or external RR reference before they can be trusted.
- 2026-06-11 automatic Stream Probe capture now unblocks raw packet collection for this path: the `stream_probe.auto` session captured K20/K21/K26 plus K18 reference frames and beat-interval discovery candidates. HRV remains blocked because K20 channel alignment is not validated across enough HR/RR reference segments, not because the app cannot capture raw stream packets.
- 2026-06-11 30-minute automatic Stream Probe capture confirms raw packet collection and K20 HR alignment are now partly unblocked: the finished 20:30:52Z-21:00:52Z probe captured 1,798 realtime K20 frames, 1,799 realtime K21 frames, 1,279 historical K20 frames, 120 K26 frames, and 3,141 trusted K18 HR reference frames. Beat-interval discovery found 42,979 direct RR-like values and 53,591 peak-spacing candidates. After time-sliced K20 analysis, offset 226 positive matched K18 HR in 5/6 slices within 8 bpm. HRV remains blocked on true RR validation because HR alignment alone does not prove beat-interval accuracy.
- 2026-06-11 RR reference capture is now implemented as a separate More > Developer route for standard BLE Heart Rate Service devices. HRV promotion remains blocked until a simultaneous automatic Stream Probe plus RR reference capture is collected, exported, and compared against K20/K26 candidate intervals.
- 2026-06-12 K20 byte/scalar discovery is now implemented as a bridge/CLI report and can rank sampled K20 fields against nearby trusted HR while waiting for RR reference hardware. This helps prioritize offsets and command contexts, but it does not unblock HRV because HR correlation alone cannot prove beat-to-beat RR intervals.
- 2026-06-12 applying K20 byte/scalar discovery to the latest full export ranked optical channel 0 sample bytes highest, while the K20 waveform scanner still ranked channel 1/offset 226 highest but below threshold. This keeps the next blocker focused on RR-reference validation and improved waveform/channel transforms rather than hunting for a simple K20 metadata scalar.
- 2026-06-12 K20 waveform transform scanning now finds a stronger HR-aligned lead on the 20:30:52Z-21:00:52Z Stream Probe: channel offset 226, negative polarity, 25 Hz, smoothing 5, threshold 0.65, 6/6 HR-matched slices inside 8 bpm, mean HR error 0.6 bpm, median candidate RR around 920 ms. HRV remains blocked because there were 0 overlapping RR-reference samples, so this is still a lead rather than validated beat-interval evidence.
- 2026-06-12 Beat Evidence Report is implemented as the one-button diagnostic summary for packet deltas plus K20/K26 beat-evidence scans. Current status on the latest probe is `candidate_hr_aligned_needs_rr_reference`: K20 and K26 leads exist, but validated RR-reference overlap is still required before HRV/recovery promotion.
- 2026-06-12 latest 30-minute automatic Stream Probe export confirms the capture path is producing the right raw surface: 3,577 K20 frames, 80 K26 frames, and a K20 waveform candidate at offset 1292 that matched trusted HR in 10/11 slices within 8 bpm. HRV/recovery remain blocked because the same bundle had 0 RR reference samples, so the lead cannot be promoted beyond diagnostic-only.
- 2026-06-13 desktop debugger run partly unblocked RR reference evidence: H6M `180D/2A37` capture stored 879 RR-reference samples from 15:03:45Z to 15:16:55Z with RMSSD ~71 ms. HRV remains blocked because the concurrent band-side desktop sessions wrote hello, high-frequency history sync, and live physiology commands but produced no custom notify packets/K20/K26 frames to validate against that RR reference.
- 2026-06-13 iOS plus desktop H6M overlap partly unblocked K20 validation: 323 RR-reference samples and 286 realtime K20 frames overlapped from 16:44:06Z to 16:49:40Z. The best simple K20 sequence candidate matched 263/287 reference beats and had RMSSD error 3.9 ms, but emitted 629 extra peaks with only 0.295 precision. K20 peak inspection shows weak threshold/prominence separation and about 3.2 surrounding candidates per beat window. Runtime-eligible score/refractory sweeps still only matched 79-135/287 beats, and a naive HR-anchored expected-window decoder matched only 36-56/287 beats. Fitting the H6M reference timeline from RR interval continuity is a real improvement: the fitted reference plus reference-assisted K20 offset/window sweep selected offset +600 ms, covered 204/289 fitted reference beats, and reduced RMSSD error to 31.3 ms. A half-window stability check is not yet convincing: the first half found offset -1300 ms with 18.1 ms RMSSD error, while the second half did not clear fitted-sweep thresholds. HRV remains blocked because this is still reference-assisted, not segment-stable, ~70.6% full-window coverage, and not a runtime-safe decoder.
- 2026-06-13 21:44:53Z-21:49:53Z repeat overlap confirmed the blocker is now precision/selection, not capture availability: the app captured 298 realtime K20 frames and the desktop H6M session contributed 503 RR samples. K20 offset 1292 negative/25 Hz matched 399/410 reference beats with 2.6 ms RMSSD error, and offset 1492 negative/25 Hz matched 398/410 with 8.3 ms RMSSD error, but both emitted ~557-563 extra peaks and stayed around 0.415-0.417 precision. HRV remains blocked until the decoder can choose one trustworthy peak per beat cycle without using the external reference.
- 2026-06-13 self-period, threshold, and spacing pruning did not clear the blocker. Self-period expected-window filters, threshold multipliers 0.35-1.25, and minimum peak spacing 8-18 samples all stayed around ~0.52 precision or worse once true-beat coverage was considered. The next candidate decoder should inspect multi-channel K20 agreement, waveform morphology, or another packet/field rather than another single-channel threshold/refractory sweep.
- 2026-06-13 multi-channel K20 agreement and basic morphology inspection did not clear the blocker either. Consensus across K20 channel offsets stayed around ~0.53 precision, and matched-vs-extra candidates had nearly identical width, symmetry, SNR, area, threshold, and prominence medians. HRV now needs a different packet/field interpretation, a better phase-within-cycle sequence model, or repeated captures that prove a stable non-reference timing rule.
- 2026-06-13 ordinal phase selection did not clear the blocker. Every-second-candidate selection improves variability shape (`SDNN` error near 2.7-5.4 ms, `RMSSD` error near 58 ms), but precision remains below 0.50 and reference coverage around 0.58. The same session cannot validate K26 because K26 had 0 frames in the exact overlap and only 1 frame in the wider 21:40Z-21:55Z window.
- 2026-06-14 K18 HRV validation is partially unblocked but not yet promotable. A 00:39:08Z-00:46:38Z iOS K18 device-time window overlapped desktop H6M cleanly; unfiltered K18 overestimated RMSSD (84.7 ms vs H6M 54.1 ms), but excluding rows already flagged `normal_history_k18_rr_heart_rate_mismatch` produced near-parity (K18 RMSSD 51.5 ms, SDNN 75.4 ms, mean NN 827.6 ms vs H6M RMSSD 54.1 ms, SDNN 77.2 ms, mean NN 825.0 ms). HRV remains blocked on repeat validation and a formal trusted K18 quality gate; K20 in the same 00:36:46Z-00:47:23Z window still failed sequence validation with precision around 0.38 and RMSSD error above threshold.
- 2026-06-14 K18 HRV now has a repeatable validation-only bridge/CLI report. The latest paired iOS/H6M database passes `k18-hrv-validation` with the warning-free K18 gate: 397 gated K18 intervals vs 545 H6M intervals, RMSSD error 2.6 ms, SDNN error 1.8 ms, mean NN error 2.6 ms, and 5-second binned RR correlation 0.887. HRV is still blocked from product promotion until this holds on at least one independent repeat capture and the trusted-input contract is updated intentionally.
- 2026-06-14 `1781404406` repeat sweep did not clear the blocker. After merging the newest iOS export with the desktop H6M database, only the existing 00:39:08Z-00:46:38Z window passed; the newest 02:24Z run had only 57 gated K18 intervals and 50 overlapping H6M intervals because K18 device timestamps stopped at 02:25:29Z while the H6M session ran until 02:33:28Z. Human/device action still needed: run another 10-15 minute H6M plus iOS capture and wait for K18 historical device time to catch up to the full H6M window before exporting.
- 2026-06-14 `1781407105` repeat sweep produced enough overlap but failed parity. The exported SQLite passed integrity despite a manifest lock warning. The fresh 03:03Z H6M session had 987 RR samples and the K18 comparison window had 390 gated K18 intervals vs 832 H6M intervals, but K18 under-matched variability: RMSSD 93.2 ms vs 117.7 ms, SDNN 89.4 ms vs 120.1 ms, 5-second binned RR MAE 35.3 ms, correlation 0.694. Constant clock offset did not rescue the run. HRV remains blocked on additional repeat captures and quality-gate refinement.
- 2026-06-14 `1781409245` repeat sweep also failed parity and showed why "wait for K18 to catch up" matters. The iOS app captured continuous K20/K21 rows from 03:36Z-03:44Z, but no K18 rows until a 03:45Z historical burst backfilled sample times starting at 03:35:10Z. The adjusted K18 report had 382 gated K18 intervals vs 778 H6M intervals; RMSSD was close (100.8 ms vs 93.5 ms), but SDNN and mean NN failed (84.6 ms vs 130.4 ms, 1042.0 ms vs 975.3 ms). K20 remained diagnostic-only in the same window: best sequence precision was 0.309 with 1,990 extra peaks and 91.6 ms RMSSD error.
- 2026-06-14 `1781449854` unblocked the K18 catch-up/window issue but not HRV promotion. The frozen 15-minute target passed K18 export readiness, but HRV failed. A diagnostic local-continuity gate recovered many rejected K18 rows and made aggregate RMSSD look much better, yet H6M row labeling showed most recovered rows in the latest run were not locally close to the reference. HRV remains blocked on repeat captures that can distinguish true long-interval variability from false long K18 excursions without using the H6M row labels as a runtime oracle.
- 2026-06-14 `1781452908` confirmed the blocker is interval classification, not export timing. K18 readiness passed for the latest 15-minute H6M overlap, but the current K18 gate still underreported variability and failed RMSSD, SDNN, mean NN, and binned MAE. Local-continuity gates can make aggregate HRV look close, but segment-level binned MAE stays high, so promotion remains blocked until the classifier can pass local agreement without using H6M as an oracle.
- 2026-06-14 `1781469645` confirms K18 is making meaningful progress under rest-like conditions but is not yet promotable as an all-day HRV source. The frozen 15-minute target passed K18 readiness and nearly passed HRV, failing only SDNN: gated K18 RMSSD 36.0 ms, SDNN 71.8 ms, mean NN 699.4 ms versus H6M RMSSD 44.8 ms, SDNN 93.2 ms, mean NN 699.7 ms, binned MAE 19.8 ms, correlation 0.887. A cleaner rest-like overlap from 20:24:01Z-20:26:56Z passed all current gates with SDNN error 0.6 ms and binned MAE 7.8 ms. HRV remains blocked on a runtime-safe rest-window/quality selector with enough contiguous K18 RR coverage; the selector can use K18/K21 context but must not use H6M row labels as an oracle.
- 2026-06-14 `1781471908` shows the current K18 gate is now too strict for a low-motion, high-variability rest window. The frozen 15-minute target passed K18 readiness and K21 motion was quiet for most of the overlap, but the current warning-free plus per-interval 8 bpm HR-marker gate failed every checked subwindow. Raw plausible K18 nearly matched the H6M aggregate metrics (RMSSD 138.2 ms, SDNN 137.0 ms, mean NN 891.6 ms versus H6M RMSSD 130.6 ms, SDNN 133.7 ms, mean NN 888.6 ms), while the current gate underreported variability (RMSSD 86.3 ms, SDNN 101.5 ms). HRV remains blocked on replacing the hard HR-marker mismatch rejection with a rest-aware, non-oracle artifact classifier that preserves real high variability and still passes local binned agreement.
- 2026-06-11 full local bundle adds K26 pulse-information as a real beat-interval lead, but it is still below the validation bar: broad direct/peak scans were ~32%/~30% within 8 bpm, and the focused byte-field scan found no promotable field/scale. Best variable HR-like field was 121/161 within 8 bpm; best RR-like field was 57/96; offset 59 big-endian RR was 370/901. K26 raw-field correlations mostly point at suspected tail/header metadata, while the best non-tail body fields are only about 0.36.
- Capture enough trusted RR-interval evidence to validate HRV, Recovery, and stress readiness.
- Next evidence target is K18 rest/segment classifier repeat validation. The Rust K18 HRV report now exposes diagnostic `raw_plausible`, `bounded_plausible_560_1300`, `local_continuity_260ms`, and `diagnostic_rest_segment_summaries` with first-pass K18-only quality flags. The K18-only filter rejects the older low-motion-looking false positives across `1781449854` and `1781469645`, but still over-selects some early `1781471908` windows that fail H6M binned agreement. More threshold tweaks on the same evidence risk overfitting; the immediate human/device blocker is another low-motion or overnight-like paired iOS/H6M capture to validate whether the K18-only quality filter repeats and to expose the final discriminator. K20 remains a secondary lead until it can choose one trustworthy peak per beat cycle without external reference help.
- Command-gated streams remain blocked for live device sends until the selected command gates are ready. The Rust capture plan now defines the order: read-only baseline config, temporary stream toggles, persistent config only with explicit approval and rollback, then shutdown. The More > Developer Stream Probe Plan route can render this plan and run packet-family delta analysis over existing evidence, but it must not send live device commands yet. After gates are ready, run the probe with packet capture enabled and export the resulting bundle for packet-family delta and beat-candidate validation.
- Compare the resulting local sleep/recovery outputs against the same date's trusted user-visible reference values.
- Current fallback is to show unavailable or low-confidence packet-derived states rather than fabricating sleep, recovery, or HRV values.

### Recovery V2 Packet Semantics

Status: blocked on human/device evidence.

- Capture and label one overnight window where the device companion app shows respiratory rate, SpO2, and skin/wrist temperature for the same date.
- Confirm whether the K18/K24 respiratory-rate candidate is a nightly average, point sample, or rolling value.
- Confirm whether wrist temperature should display absolute skin temperature or deviation from baseline.
- Identify which packet/field should be trusted for SpO2; Rust currently treats oxygen saturation as decoder-not-implemented.
- Decide whether Recovery timeline should eventually link to a persisted Rust algorithm-run/input-id record, rather than the current UI-level score/sleep/metric/blocker rows.

## How To Add Future Blockers

When a request cannot make meaningful progress without human input or action, add an entry here with:

- The feature or request name.
- The exact human action needed.
- The current fallback or safe behavior.
- Any file, capture, label, credential, or decision that would unblock the work.
