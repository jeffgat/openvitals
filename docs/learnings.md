# OpenVitals Learnings

This file is the running memory for durable lessons we learn while building OpenVitals. Add to it when a discovery would save a future agent or contributor from rediscovering context, constraints, tradeoffs, or validation rules.

## How To Add A Learning

Use short entries. Include the area, the context, the learning, and any follow-up.

```md
### Area: Short Title

- Context: What prompted the discovery.
- Learning: What should be remembered.
- Follow-up: What should happen next, if anything.
```

## Product And Positioning

### Public Positioning Is Brand-Neutral

- Context: The app is internally informed by reverse-engineering work for a WHOOP 5 and subscription-free personal use.
- Learning: Publicly, OpenVitals should be described as a local-first, general purpose BLE health monitor for compatible user-owned wearables. Do not mention WHOOP in user-facing copy unless the user explicitly changes the positioning.
- Follow-up: Audit new UI, README changes, release notes, screenshots, support copy, and About/Privacy surfaces for brand-neutral wording.

### This Is Personal-Use Infrastructure First

- Context: The app is being built for ourselves, friends, and family, not as a monetized competitor.
- Learning: Product choices should favor local ownership, transparent data provenance, careful health language, and practical device utility over growth, lock-in, or marketing polish.
- Follow-up: Keep privacy and export flows understandable before adding any network or AI feature.

## Data And Trust

### Missing Data Is A Real State

- Context: The existing MVP docs repeatedly require empty, stale, and unavailable states.
- Learning: OpenVitals must not invent health metrics. A metric should render only when it has live, local, bridge-derived, imported, user-entered, or explicitly stale data with provenance.
- Follow-up: When adding a surface, design its no-data state at the same time as its populated state.

### Provenance Is Product Surface, Not Debug Noise

- Context: Health, Coach, More, and the Rust core all track source, readiness, validation, and next actions.
- Learning: Provenance should stay visible enough that users and developers can tell why a number exists and what evidence is missing.
- Follow-up: Prefer typed provenance fields over summary strings when touching data models.

### Raw Evidence Is Sensitive

- Context: The Rust core includes capture sanitization, raw export, export validation, and privacy lint tooling.
- Learning: Packet payloads, captures, command evidence, logs, identifiers, HealthKit records, and labels should be treated as sensitive artifacts.
- Follow-up: Keep raw bytes in operational/debug/export flows and run privacy lint before sharing export bundles.

## Architecture

### Swift Owns UI And Device Session State

- Context: `OpenVitalsBLEClient`, `OpenVitalsAppModel`, and SwiftUI views manage Bluetooth state, app lifecycle, pipelines, and presentation.
- Learning: Swift should own user interaction, navigation, live session state, and view composition.
- Follow-up: Keep Home, Health, More, and Coach modular; avoid letting one tab depend on another tab's view internals.

### Rust Owns Protocol, Validation, Storage, And Algorithms

- Context: `Rust/core` contains parser, metric, store, export, command validation, calibration, and bridge modules.
- Learning: Packet schemas, local metric formulas, readiness gates, command validation, export validation, privacy lint, and SQLite contracts should stay in Rust when possible.
- Follow-up: Use `OpenVitalsRustBridge` to request bridge outputs instead of duplicating Rust-owned logic in Swift.

### The Bridge Is JSON-Oriented

- Context: `OpenVitalsRustBridge` sends `open_vitals.bridge.request.v1` JSON requests over the C ABI and decodes JSON responses.
- Learning: Bridge methods should be batch-oriented and return renderable values with explicit errors rather than panicking or relying on native exceptions.
- Learning: Batched bridge summaries need the same typed arg shape as their single-method equivalents; passing unavailable-status args into a rollup slot can make Swift persist/render unavailable rows even when promotable metric values exist.
- Follow-up: When adding a bridge method, update Rust tests, Swift call sites, and docs together.

### High-Volume Sync Must Batch UI Work

- Context: Band historical sleep sync can deliver many packet notifications before the final metadata arrives.
- Learning: Pure historical data packets should be coalesced before touching main-thread `@Published` state, and bridge-backed score recomputation should run off the main actor.
- Follow-up: When adding packet-stream features, batch status updates and keep Rust bridge calls on a background queue unless the method is known to be trivial.

### Hot Paths Need Bounded Work

- Context: Sleep sync, packet inputs, diagnostic logging, and live HR/HRV sampling can all run while the UI is visible.
- Learning: Hot paths should avoid whole-database scans, repeated bridge round trips, immediate file synchronization, and whole-file JSON rewrites. Prefer bounded windows, batched bridge responses, buffered writes, and append-oriented persistence.
- Learning: Rolling packet-capture summaries and routine bridge timing breadcrumbs should update lightweight status state or snapshot files, not the displayed event log, OSLog, console stream, diagnostic file log, or overnight event spool.
- Learning: Long-running automatic passive captures should not persist at the same cadence as explicit debug captures or active activities; sample passive evidence and coalesce SQLite/Rust bridge imports so UI navigation stays responsive while live parsing continues.
- Learning: SwiftUI dashboard bodies should compute packet-backed snapshot bundles once per render and pass the results down; repeatedly calling `landingSnapshots`, `healthMonitorSnapshots`, or stress/energy summaries from multiple card sections can multiply main-thread work after a Health refresh.
- Learning: Repeated iOS "System gesture gate timed out" console lines after a Health refresh are strong evidence that SwiftUI/main-actor work is starving touch handling; cache packet-backed landing summaries and keep live packet ingestion off render paths before chasing BLE reconnect logic.
- Follow-up: When adding live packet or diagnostic features, define the time window, batching cadence, and persistence strategy before wiring UI refreshes.

## Current Surface Map

### Tabs Are Uneven By Design Right Now

- Context: `AppShellView` defines Home, Health, Coach, and More, but `bottomTabs` currently includes Home, Health, and More while Coach is commented out.
- Learning: Coach code exists, but the user-facing tab shell does not currently expose it as a bottom tab.
- Follow-up: If Coach is re-enabled, check privacy, deterministic recommendation boundaries, and routing before exposing free-form chat behavior.

### Health Is The Most Complete MVP Surface

- Context: `docs/open-vitals-mvp/Health.md` is mostly checked off and references screenshot evidence from 2026-06-01.
- Learning: Health has the strongest implemented contract for metric details, trends, packet inputs, algorithms, references, and calibration.
- Learning: Health refresh should expose packet-input and packet-score bridge status together; otherwise a completed input run can still leave Sleep/Recovery/Strain cards looking unchanged until score recomputation runs.
- Learning: Sleep, Recovery, and Strain cards should reload the latest persisted algorithm runs on launch so restart does not turn previously computed scores into unavailable states.
- Learning: The Home daily-score Sync button should request band historical packets first, then run packet input extraction and packet-score recomputation; Sleep, Recovery, and Strain all depend on packet-derived inputs even though Sleep exposes the most obvious manual sync entry.
- Learning: Historical sync packet counts are not enough for metric readiness; Home score sync needs an active local capture/import session so received historical frames are persisted into SQLite before packet inputs and scores run.
- Learning: A 2026-06-11 manual local bundle had enough motion/window evidence for `open_vitals.sleep.v0`, while `open_vitals.sleep.v1` stayed blocked by its release-gate semantics; Swift should request sleep v0 until v1 is promoted.
- Learning: A 2026-06-11 still physiology capture produced K20/K21/K18/K2 frames but zero K17/R17 frames; a later bundle showed the Labrador data-generation/raw-save/filtered toggles were attempted but returned result code 0 and still produced no trusted beat-interval evidence.
- Learning: HRV readiness should talk about validated beat-interval evidence, not a single guessed packet family; K17 remains one candidate, while K16/raw ECG and K20/raw research bodies need discovery scans before any HRV promotion.
- Learning: A 2026-06-11 K20-vs-HR validation over the aggregate diagnostic capture found K20 raw/research candidates do not align with trusted HR: direct i16 plausible values averaged ~59 bpm against ~99 bpm HR with only 2/110 matches inside 8 bpm, while peak spacing averaged ~176 bpm with 0/110 matches.
- Learning: The 2026-06-11 full local bundle shows K18 is the trusted heart-rate history reference, while K20 raw/research and K26 pulse-information are the active beat-interval search surfaces; K17/R17 is absent as a data-packet stream.
- Learning: K26 pulse-information is a better lead than K20 but still not promotable. Across the full 2026-06-10/11 bundle, K26 direct i16 candidates matched nearby trusted HR within 8 bpm in ~32% of matched frames and peak-spacing candidates in ~30%, below the 80% promotion threshold.
- Learning: A focused 2026-06-10/11 K26 byte-field scan matched all 901 K26 frames to trusted K18 HR, but no field/scale was promotable: the best variable HR-like field was offset 53 little-endian width 2 scaled by 0.2 at 121/161 within 8 bpm, the best RR-like field was offset 58 little-endian width 4 scaled by 0.5 at 57/96, and the old offset 59 big-endian RR hunch was only 370/901. Keep K26 diagnostic-only until a stronger transform or external RR reference validates it.
- Learning: K26 raw-field correlation is also not enough to justify HRV promotion. The strongest correlations in the 2026-06-10/11 bundle are tail/header-looking fields: offsets 58/59/60 correlate with HR around -0.78 but are flagged as suspected tail metadata with low-cardinality values, while the best non-tail body fields are only about 0.36. Next beat-interval discovery should prioritize K20 field scans and alternate command-gated optical/filtered streams over single-field K26.
- Learning: A quick 2026-06-10/11 K20 raw-field correlation pass also failed to reveal a clean beat-interval field. Only 664/1832 K20 frames had nearby trusted HR; the strongest simple fields were around offset 119/123 with moderate HR/RR correlation (~0.58/0.51), and the most plausible 300-2000 raw range at big-endian offset 25 correlated positively with HR rather than like an RR interval. K20 still needs structured waveform or command-context work, not direct single-field promotion.
- Learning: The 2026-06-11 realtime raw export confirms `REALTIME_RAW_DATA` decodes into K20/K21, not hidden K10/K11/K16/K17 packets. K20 has a 2,115-byte body with six repeatable 25-sample channel blocks around offsets 26, 226, 1292, 1492, 1714, and 1914; K21 is motion with six parsed 100-sample axes.
- Learning: The 2026-06-11 realtime K20/K26 validation still does not unblock HRV. Across 2,600 K20/K26 candidate frames, direct i16 plausible intervals matched nearby trusted HR in only 327/1,433 matched frames within 8 bpm, while peak spacing matched 247/1,121; K20 channel-peak scans can approximate session HR in one later window but produce noisy intervals and fail consistency across windows.
- Learning: The K20 optical-channel scan should stay diagnostic-only until validated against RR evidence. On the 2026-06-10/11 local bundle it found 1,832 K20 frames, 1,361 realtime K20 frames, 58 candidate channel segments, and 34 HR-matched segments, but the best offset-26 positive channel only matched 3/5 usable segments with 33% inside 8 bpm and ~14 bpm mean absolute error.
- Learning: A 2026-06-11 short capture window from 19:10-19:30Z produced 258 decoded frames but only `REALTIME_DATA` K2 plus one event; no K20, K21, K26, or trusted K18 appeared in that window. K20/HRV probes need diagnostic/physiology raw capture or a validated command-gated stream, not walk-mode realtime data alone.
- Learning: The 2026-06-11 automatic Stream Probe diagnostic capture worked operationally: it produced a finished `stream_probe.auto` session from 19:48:34Z to 19:59:49Z with 7,782 decoded rows, including 776 K20, 839 K21, 226 K26, and 5,205 K18 reference frames. Beat-interval candidate discovery passed with 9,963 direct RR-like values and 10,452 peak-spacing candidates, but K20 channel validation stayed diagnostic-only because the short window had only one matched HR segment and the broader day scan remained below the HR-alignment threshold.
- Learning: A longer 2026-06-11 automatic Stream Probe capture also worked: the finished 20:30:52Z-21:00:52Z `stream_probe.auto` session produced 12,147 frames, including 1,798 realtime K20 frames, 1,799 realtime K21 frames, 1,279 historical K20 frames, 120 K26 frames, and 3,141 trusted K18 HR reference frames. Beat-interval discovery found 42,979 direct RR-like values and 53,591 peak-spacing candidates, but K20 channel validation still had only one usable matched segment; the next engineering step is better channel segmentation/alignment, not just a longer capture.
- Learning: The K20 channel analyzer must time-slice long continuous streams before ranking channels. After adding two-minute validation slices, the same 2026-06-11 30-minute probe produced 177 candidate channel segments and 69 HR-matched segments; the best diagnostic candidate was K20 channel offset 226 with positive polarity, 5/6 matched slices inside 8 bpm, mean absolute error 3.9 bpm, and median candidate RR around 880 ms. This is HR-alignment evidence only and still requires an external/validated RR reference before HRV promotion.
- Learning: K20 channel scan now consumes `rr_reference_samples` when present and reports RR-reference overlap separately from trusted-HR overlap. Status `candidate_hr_aligned_needs_rr_reference` means the K20 candidate is still HR-only; status `candidate_hr_and_rr_aligned` means the candidate matched both trusted HR and stored RR-reference medians in the analysis slices, but it remains diagnostic-only until enough real-world windows are collected.
- Learning: K20 scalar/byte-field discovery is now a first-class Rust bridge/CLI report (`metrics.k20_field_discovery`, `k20-field-discovery`). It samples large K20 windows, ranks raw byte/word/dword fields against nearby trusted K18 HR, labels channel-vs-metadata regions, and keeps results diagnostic-only until overlapping RR reference samples validate beat intervals.
- Learning: Running K20 field discovery on the 2026-06-11 full local export found 5,685 K20 frames, 2,863 with nearby trusted HR, and 573 sampled frames. The strongest scalar correlations were overlapping 4-byte fields inside K20 channel 0 (offsets 53/54/61/62/81/82/97/98/105/106/109/110) at about -0.79 vs HR and +0.75 vs HR-derived RR, so the useful signal appears to be optical-channel waveform content rather than a standalone RR metadata field.
- Learning: Running the K20 optical-channel scan on the same 2026-06-11 export found 415 candidate segments and 129 HR-matched segments; K20 channel 1 at offset 226 remained the best waveform lead, but only 6/11 matched slices were inside 8 bpm and no RR reference samples existed, so it stays diagnostic-only.
- Learning: Overnight command evidence confirms realtime HR, R10/R11 realtime, IMU, persistent R21, optical data, optical mode, and persistent R20 were enabled before K20/K26 appeared; future beat-interval discovery should probe adjacent optical/filtered command variants rather than assuming sleep alone emits K17.
- Learning: K20 waveform transform scan is now a first-class Rust bridge/CLI report (`metrics.k20_waveform_transform_scan`, `k20-waveform-transform-scan`) and sweeps sample-rate, spacing, smoothing, threshold, channel, and polarity parameters. On the 2026-06-11 20:30:52Z-21:00:52Z probe it improved the best K20 lead to channel offset 226, negative polarity, 25 Hz, smoothing 5, threshold 0.65, with 6/6 HR-matched slices inside 8 bpm and mean HR error 0.6 bpm, but still no RR-reference overlap.
- Learning: Beat Evidence Report is now a combined bridge/CLI/app report (`metrics.beat_evidence_report`, `beat-evidence`) that runs packet delta, K20 waveform transform scan, K20 channel scan, K20 field discovery, and K26 field scan together. It can identify HR-aligned leads in one place while keeping HRV blocked until true RR-reference samples validate beat intervals.
- Learning: Command-gated stream discovery now belongs in the Rust command capture plan: baseline read-only optical/research config first, temporary stream toggles next, persistent R20/R21/research config last, and shutdown commands after raw stream probes. Each step should declare expected packet-family deltas before it is sent.
- Learning: The user-facing Debug Capture action should run an aggregate diagnostic capture rather than forcing users to choose between movement, physiology, and temperature probes; keep specialized modes internal for targeted experiments and overnight flows.
- Learning: A 2026-06-12 30-minute automatic Stream Probe export produced a stronger K20 waveform lead: channel offset 1292, positive polarity, 20 Hz, smoothing 5, threshold 0.85, 10/11 HR-matched slices inside 8 bpm, mean HR error 5.5 bpm, and median candidate RR around 750 ms. It still has 0 RR-reference samples, so it remains diagnostic-only and cannot feed HRV/recovery yet.
- Follow-up: Use Health patterns for provenance and unavailable states when improving Home, More, or Coach.

### More Is The Operational Home

- Context: `docs/open-vitals-mvp/More.md` assigns device, connection lab, capture, local store, health sync, raw export, algorithms, debug, privacy, and support to More.
- Learning: Low-level BLE controls, command gates, raw packets, exports, privacy lint, storage checks, and destructive actions belong in More or Debug-style routes.
- Learning: Clearing local app data is separate from forgetting a remembered BLE device; destructive wipe actions should preserve reconnect keys unless the user explicitly chooses a device-forget path.
- Learning: Remote debug uploads should reuse the explicit local data bundle, require a manual user action, use anon-key RLS/policies, and never embed a service-role key in the app.
- Learning: Remote debug upload UI needs step-specific labels for settings, object upload, manifest upload, and metadata insert; Supabase storage limits can fail before any database row exists.
- Learning: Supabase Free projects cap Storage object uploads at 50 MB globally, so large overnight debug bundles need compression or chunked upload rather than a larger bucket setting.
- Learning: Stream Probe Plan belongs in More/Developer as an analysis surface first: render the Rust capture plan, compare packet-family deltas across probe and baseline windows, and keep live command sends behind command validation.
- Learning: Packet-family deltas for data streams should classify frames from parsed payload `packet_k`; command response names are useful context, but they do not prove which data-packet family was emitted.
- Learning: The app now has a separate RR reference peripheral route under More > Developer > Stream Probe Plan. It scans for standard BLE Heart Rate Service `180D` devices, subscribes to measurement characteristic `2A37`, stores RR intervals in `rr_reference_samples`, and keeps that evidence validation-only rather than promoting it directly into user-facing HRV.
- Learning: Desktop BLE reverse-engineering should reuse the Rust bridge as the parser/import source of truth instead of reimplementing packet schemas in TypeScript. The `Tools/ble-packet-debugger` Electron app runs BLE capture in a plain Node host process, calls `open-vitals-bridge --stdio`, writes `raw_evidence`/`decoded_frames` through `capture.import_frame_batch`, and treats standard Heart Rate `180D/2A37` straps as raw/reference evidence unless a Rust metric contract promotes them.
- Learning: Electron 33 sandboxes preload scripts by default, which can prevent a preload that imports local shared modules from exposing the IPC bridge. The desktop BLE debugger keeps `contextIsolation: true` and `nodeIntegration: false`, but sets `sandbox: false` so `preload.js` can import the shared IPC constants and expose `window.openVitalsDebugger`.
- Learning: RR reference capture must connect and subscribe to the BLE reference device before waiting on local SQLite session creation. If storage is locked or slow, RR samples should still appear live and queue for later insertion; otherwise the UI can show a selected reference device while no notifications are actually subscribed.
- Learning: Automatic Stream Probe should not immediately create the full local data bundle while RR reference capture is still flushing. The large export can compete with local SQLite/session writes and leave valid live RR samples queued but not stored; stop the probe, let RR storage finish, then run Raw Export manually.
- Learning: The RR-reference validation workflow should be a guided flow, not two independent debug tools. The normal path is: scan/select reference strap, wait for real RR samples, start the band probe, stop the reference capture after the probe, wait for stored RR samples, then let the user manually create/share the local bundle.
- Learning: In the 2026-06-13 desktop BLE HRV run, `WBB5BP0151798` was the charger/power accessory and not the band. The desktop debugger should keep charger-like WBB names as unknown unless they advertise the custom band service, and should target the `WHOOP 5` band for protocol captures.
- Learning: The desktop debugger should mirror the app's sensor-stream command sequence starting at 180 for live physiology and high-frequency history sync writes. The 2026-06-13 desktop run collected H6M RR-reference samples, but the band-side desktop sessions produced only command-write evidence and no custom notify packets from `fd4b0003/0004/0005/0007`.
- Learning: Desktop command-path parity with iOS requires sending the fixed `CLIENT_HELLO`/`GET_HELLO` frame as soon as the V5 command characteristic is ready, using the same write-type preference as Swift (`withResponse` when available), then delaying automatic physiology start by 5 seconds. The debugger now exposes this as an iOS parity physiology probe for future captures.
- Learning: Desktop band matching must prefer advertised custom band services before WBB/name-based power-accessory filtering. WBB-like peripherals without the custom service can stay unsupported/nearby, but a custom-service match must remain selectable and should surface custom-service evidence in the device row.
- Learning: A useful desktop parity probe is a single clean flow: stop prior scan/capture, scan for a custom-service band, connect without auto-hello, start capture before writes, verify `fd4b0002` plus `fd4b0003/0004/0005/0007` subscriptions, send hello, wait 5 seconds, send the iOS physiology sequence, and treat fd4b custom notify packets as success. Command writes or HR strap `2A37` packets alone do not prove band-side capture.
- Learning: Desktop debugger diagnostics must separate OS-level write acceptance from band-side protocol ACKs. If `GET_HELLO` produces no parsed fd4b command response, the probe failed at the handshake/notification layer and should not continue as though physiology stream decoding was the primary problem.
- Learning: Desktop fd4b notify diagnostics need separate request and confirmation states. A probe with `fd4b Req 4/4` but `fd4b Notify 0/4` means the desktop attempted to enable all custom notify characteristics but CoreBluetooth/noble did not confirm notification state; that should be diagnosed separately from missing command responses or data frames.
- Learning: Do not directly write the Client Characteristic Configuration descriptor from the macOS desktop debugger. CoreBluetooth throws `Client Characteristic Configuration descriptors must be configured using setNotifyValue:forCharacteristic:` and aborts the Node BLE host if the noble descriptor write path is used for CCCD.
- Learning: A native macOS CoreBluetooth probe on 2026-06-13 requested all four fd4b notify characteristics and sent the same `GET_HELLO` frame, but CoreBluetooth reported `Encryption is insufficient` for `fd4b0003`, `Authentication is insufficient` for `fd4b0004/0005/0007`, and `Authentication is insufficient` for the `fd4b0002` write. Desktop custom notify is blocked on authenticated/encrypted pairing/bonding, not Rust parsing or the physiology command sequence.
- Learning: A compatible band can advertise the custom `fd4b0001` service continuously to noble/CoreBluetooth while macOS Bluetooth Settings does not list it as a pairable/registerable accessory. Treat this as "advertising but unregistered" rather than "not nearby"; secure fd4b notify/write access still needs a bonded/authenticated Mac link or a private onboarding path.
- Learning: The desktop noble subscribe callback must be treated as diagnostic evidence. Ignoring that callback collapses authentication/encryption errors into vague missing notify state and sends debugging toward packet parsing when the BLE link security is the actual blocker.
- Learning: The desktop debugger should expose the native CoreBluetooth auth probe in the UI, not just as a CLI script. The useful sequence is to release the active noble scan/capture/connection, run the Swift probe, stream its lines into the debugger log, and then open Bluetooth Settings for pairing/bonding if authentication/encryption is required.
- Learning: The desktop debugger must not default Electron dev mode to a common Vite port. `npm start` should force the built renderer, and `npm run dev` should set an explicit debugger-owned `VITE_DEV_SERVER_URL`; otherwise Electron can silently load an unrelated localhost app and make UI validation meaningless.
- Learning: Raw/local exports must snapshot SQLite with `sqlite3_backup` instead of copying the live database file directly. A 2026-06-12 local bundle included a structurally valid manifest but an embedded `open_vitals.sqlite` whose `raw_evidence` and `decoded_frames` tables failed `pragma integrity_check`; `.recover` salvaged the data, but future exports should ship a clean snapshot and skip WAL/SHM sidecars.
- Learning: Historical `GET_DATA_RANGE` can time out even when the device is still capable of sending packet bodies. Full sync should degrade to a direct packet-transfer request after bounded range retries, while range-only diagnostics should still fail loudly.
- Learning: Historical sync can resend overlapping packet bodies, so durable import identity must not depend on BLE receive time. Store a stable local sync identity from device key, historical packet type/K, device timestamp, and payload hash; skip repeats as already synced and advance the local checkpoint only from accepted historical packets.
- Learning: Device-side historical cursor sync is not safe to claim from timestamps alone. Command 33 is mapped as `set_read_pointer`, but the payload shape and timestamp/page semantics are not validated in local evidence, so dry-run must block requested cursor mode until an owned command 33 payload capture proves how to build it.
- Learning: Large local data exports need staged UI progress. Snapshotting SQLite, base64 bundle writing, validation, and final manifest hashing can each take visible time; show phase labels and determinate byte progress where possible instead of a generic spinner.
- Learning: SQLite snapshot export must bound `SQLITE_BUSY`/`SQLITE_LOCKED` waits and report `sqlite3_backup` page progress. Active capture/probe writers can otherwise leave the local data export stuck at the snapshot phase with no visible movement or actionable error.
- Learning: K20 beat-sequence validation must compare detected beat timestamps against external RR-reference beat timestamps, not just median HR/RR. On the 2026-06-13 iOS plus desktop H6M overlap, the best K20 lead was channel offset 1492, negative polarity, 25 Hz, smoothing 5, threshold 0.25, and clock offset +2000 ms. It matched 263/287 reference beats with RMSSD error 3.9 ms, but emitted 892 candidate peaks with 629 extras, so precision was only 0.295 and HRV remains diagnostic-only.
- Learning: K20 peak inspection on the same overlap shows threshold/prominence separation is weak: matched and extra candidate peaks have similar threshold ratios/prominence, with about 3.2 surrounding candidates per beat window. The next decoder should use one-peak-per-beat clustering, dynamic refractory spacing, or expected-beat-window selection instead of relying on a simple higher threshold.
- Learning: A score-based refractory sweep does not solve the 2026-06-13 K20/H6M overlap. Runtime-eligible refractory windows from 450-900 ms selected 194-358 candidates but only matched 79-135/287 reference beats, while reference-assisted upper bounds matched about 260/287. This means the K20 signal has alignable beat peaks, but local score/refractory/phase rules are insufficient; the next runtime decoder needs an expected beat window anchored by trusted HR/period continuity or another timing source.
- Learning: A naive HR-anchored expected-window decoder also does not solve the 2026-06-13 K20/H6M overlap. Runtime-eligible HR grids selected phases around 680-760 ms and matched only 36-56/287 reference beats. A reference-assisted phase/radius upper bound found the better 360 ms phase and matched 201/287 beats at 0.728 precision, but still had 152 ms RMSSD error. The next decoder should focus on sequence-level interval/timing calibration or a different packet field, not just more threshold, refractory, or median-HR phase search.
- Learning: Sequence-level K20 calibration improves the same overlap when the H6M reference timeline is reconstructed from RR interval continuity instead of trusting notification arrival timestamps. The fitted H6M timeline removed impossible close beats; a reference-assisted K20 offset/window sweep selected offset +600 ms and an 800 ms window, covered 204/289 fitted reference beats, and reduced RMSSD error to 31.3 ms. This is a real convergence signal, but still validation-only: coverage is ~70.6%, the selection uses reference intervals, and the runtime decoder still needs a non-reference timing rule or another packet/field before HRV can be promoted.
- Learning: The fitted K20/H6M alignment is not stable across shorter subwindows of the same 2026-06-13 overlap. The first half found a different offset (-1300 ms) with 18.1 ms RMSSD error, while the second half did not clear the current fitted-sweep thresholds. This means the full-window result is a lead, not proof; the next validation pass should require repeated overlaps or a segment-stability criterion before promoting any K20 timing rule.
- Learning: A second 2026-06-13 iOS plus H6M overlap at 21:44:53Z-21:49:53Z confirmed that K20 contains beat-correlated structure but still over-detects. The strongest sequence scan lead was K20 channel offset 1292, negative polarity, 25 Hz, smoothing 5, threshold 0.25, clock offset -2400 ms: it matched 399/410 reference beats and had only 2.6 ms RMSSD error, but emitted 563 extra peaks with precision 0.415. The older offset 1492 lead also matched many beats (388/404) with 8.7 ms RMSSD error but precision 0.406. Do not trust aggregate RMSSD alone; require one-beat-per-cycle selection/precision before HRV promotion.
- Learning: Simple one-beat pruning is still insufficient on the 21:44Z K20/H6M overlap. A self-period expected-window selector inferred plausible periods from K20 candidate spacing, but its best runtime-eligible precision stayed around 0.52 with poor match/RMSSD. Threshold sweeps from 0.35-1.25 and spacing sweeps from 8-18 samples also failed to break past ~0.52 precision without dropping many true beats. The next decoder path should look at multi-channel consensus, waveform morphology, or packet fields beyond a single K20 channel's local peak train.
- Learning: Multi-channel K20 consensus and basic peak morphology also did not clear the 21:44Z overlap. A blind consensus pass across K20 channel offsets selected 366-395 candidates but stayed around 0.51-0.53 precision with RMSSD error over 129 ms. Matched and extra peaks had effectively identical morphology medians (width 1 sample, symmetry 0, local SNR about 1.1, area about 14.7-14.8), so width/SNR/prominence/threshold-ratio filters are unlikely to separate true beats from sub-peaks. The next sane path is a different packet/field interpretation or a sequence model that explicitly chooses one phase within each optical beat cycle and proves segment stability.
- Learning: K20 ordinal-phase selection found a real but insufficient structure on the 21:44Z overlap. Selecting every second candidate peak preserved variability shape better than refractory/consensus (`RMSSD` error about 58 ms and `SDNN` error 2.7-5.4 ms on offsets 1292/1492), but precision stayed below 0.50 and matched only about 58% of reference beats. This supports the "multiple sub-peaks per optical cycle" hypothesis, but still does not provide a runtime-safe RR decoder.
- Learning: The 21:44Z overlap cannot validate K26 because it contains no K26 frames in the exact iOS/H6M window and only one K26 frame in the wider 21:40Z-21:55Z session. The next capture should explicitly aim for K26 or another richer packet family while the H6M reference is running.
- Learning: NOOP's WHOOP 5 historical v18 support maps cleanly onto OpenVitals K18 payload offsets after subtracting the 8-byte V5 header: heart rate at payload[14], RR count at payload[15], and up to four little-endian u16 RR intervals at payload[16 + 2n]. OpenVitals now decodes those K18 RR values as diagnostic `normal_history_k18_rr_candidate` evidence, but keeps them untrusted and out of HRV/Recovery score inputs until an owned iOS export with band-side decoded frames validates the same layout against RR reference evidence. Current desktop debugger databases remain command-only for the band side and cannot validate K18 HRV.
- Learning: A 2026-06-14 iOS local export plus desktop H6M run confirmed K18 RR candidates are abundant and physiologically plausible, but not yet promotable. The export had 61,686 K18 frames, 22,557 frames with plausible RR, and 24,906 RR intervals; 82.9% of plausible K18 RR frames matched frame HR within 8 bpm via `60000 / mean(RR)`. The `23:15Z` captured window produced 837 K18 candidate frames and 1,125 intervals with diagnostic RMSSD 110.3 ms, while the trusted HRV report correctly stayed blocked. H6M validation remained insufficient because K18 is historical offload: device timestamps in that capture spanned 22:47:20Z-23:19:17Z while BLE arrival was 23:15:35Z-23:20:24Z, leaving only a small actual device-time overlap with the desktop H6M session.
- Learning: A later 2026-06-14 iOS export plus desktop H6M overlap finally aligned K18 device time with the H6M reference. The 00:39:08Z-00:46:38Z K18 RR window had 349 K18 rows and 426 intervals; unfiltered K18 matched H6M mean/median RR closely but overestimated RMSSD (84.7 ms vs H6M 54.1 ms). Excluding rows already flagged `normal_history_k18_rr_heart_rate_mismatch` produced near-parity: 397 K18 intervals, RMSSD 51.5 ms, SDNN 75.4 ms, mean NN 827.6 ms, versus H6M RMSSD 54.1 ms, SDNN 77.2 ms, mean NN 825.0 ms. Treat K18 HRV promotion as blocked on repeat validation plus a formal quality gate, not on basic interval mapping.
- Learning: Current protocol classification is layered but incomplete. The latest export has one-off unknown outer packet types 94 and 166; K2 is observed but not body-decoded; K7, K9, K11, K12, K16, K19, K20, K22, K24, K25, and K26 are still domain-labeled, diagnostic-only, or partially decoded; K20/K26 do not have a trusted beat-interval body schema; metadata 49/56 and console 50/55 remain mostly raw; most command response ids are unnamed beyond `GET_HELLO`; and observed events 110, 115, 116, 120, 121, and 124 still lack stable names/body semantics. Treat these as mapping gaps and avoid metric promotion from them without separate validation.
- Learning: K18 HRV validation is now a first-class Rust bridge/CLI report (`metrics.k18_hrv_validation`, `k18-hrv-validation`). It uses K18 device timestamp seconds for historical sample time, ignores the non-millisecond K18 subsecond field when needed, rejects warning rows before parity, and compares raw/gated K18 RR intervals against stored RR-reference samples. On the 2026-06-14 00:39:08Z-00:46:38Z overlap it passed validation-only with gated K18 RMSSD 51.5 ms, SDNN 75.3 ms, mean NN 827.6 ms versus H6M RMSSD 54.1 ms, SDNN 77.1 ms, mean NN 825.0 ms; 5-second binned RR MAE was 16.4 ms with correlation 0.887. Promotion still requires repeat validation.
- Learning: The 2026-06-14 `1781404406` iOS export plus desktop H6M sweep did not provide a second K18 pass. Seven H6M windows were checked after merging desktop `rr_reference_samples`; only the existing 00:39:08Z-00:46:38Z K18 device-time window passed. The newest 02:24Z H6M run failed because K18 arrived as a short burst captured from 02:25:06Z-02:25:34Z whose device timestamps only covered 02:15:03Z-02:25:29Z, leaving 57 gated K18 intervals and 50 reference intervals. The next repeat must keep historical sync/capture running, or wait before export, until K18 device timestamps cover the full H6M run.
- Learning: The 2026-06-14 `1781407105` iOS export plus desktop H6M run is a real negative K18 repeat, not a short-overlap failure. SQLite integrity passed despite the manifest lock warning, K18 device timestamps reached 03:18:00Z, and the 03:04:39Z-03:17:44Z comparison had 390 gated K18 intervals and 832 H6M intervals. K18 still failed parity: RMSSD 93.2 ms vs H6M 117.7 ms, SDNN 89.4 ms vs H6M 120.1 ms, 5-second binned RR MAE 35.3 ms, correlation 0.694. A constant clock-offset sweep did not improve the match, so repeat validation now needs quality-gate refinement or more sessions before promotion.
- Learning: The 2026-06-14 `1781409245` iOS export plus desktop H6M run did not clear K18 HRV promotion. K18 historical rows paused from 03:36Z-03:44Z while K20/K21 realtime rows continued, then backfilled K18 sample times in bursts; the adjusted 03:34:58Z-03:47:37Z device-time comparison had 382 gated K18 intervals vs 778 H6M intervals. RMSSD stayed near tolerance (100.8 ms vs 93.5 ms), but SDNN and mean NN failed badly (84.6 ms vs 130.4 ms, mean NN 1042.0 ms vs 975.3 ms). K20 was not a rescue path in the same window: the best RR-sequence candidate matched 891/939 reference beats but emitted 1,990 extra peaks, precision 0.309, and RMSSD error 91.6 ms.
- Learning: The 2026-06-14 `1781445204` iOS export plus desktop H6M run showed K18's RMSSD failure was mostly a quality-gate issue, while the remaining failure was catch-up/window coverage. Adding an individual RR-derived HR consistency gate within 8 bpm of the K18 HR marker reduced gated K18 RMSSD from 130.7 ms to 87.1 ms versus H6M 85.5 ms, improved 5-second binned RR MAE from 21.0 ms to 19.5 ms, and kept correlation high at 0.93. The same run still failed SDNN/mean NN because K18 quality-gated sample time stopped at 13:36:13Z, 341 seconds behind the RR-reference target, so automatic probes should freeze/stop the RR reference at the target window and keep band capture open for bounded K18 catch-up before export.
- Learning: The 2026-06-14 `1781447626` iOS export plus desktop H6M run confirmed the K18 validator must separate BLE arrival time from K18 device sample time. The latest iOS capture arrived from 14:18:17Z-14:33:17Z, but K18 device timestamps only reached 14:27:05Z and quality-gated RR reached 14:27:03Z, 394 seconds behind the H6M target. HRV validation now queries decoded rows through an `observed_end` catch-up window but clips K18 RR intervals by sample time before comparing to H6M. After clipping, SDNN passed (99.0 ms vs 107.5 ms) and binned RR shape stayed strong (MAE 17.3 ms, correlation 0.971), but RMSSD and mean NN still failed (58.5 ms vs 75.2 ms, mean NN +19.0 ms). This sample is not a promotion pass. The capture still stopped exactly at 15 minutes because the underlying health packet capture had its own 15-minute timeout; automatic K18 catch-up needs a longer diagnostic-capture fail-safe than the visible probe/reference window.
- Learning: The 2026-06-14 `1781449854` iOS export showed the automatic K18 catch-up timer fix worked. The latest automatic probe session was started with a 1,560-second diagnostic capture fail-safe, ran from 14:50:40Z-15:07:19Z, and K18 export readiness passed for the frozen 15-minute reference target: latest quality-gated K18 RR sample time was 15:06:18Z versus target 15:05:40Z. The desktop H6M session continued until 15:10:51Z, so using the full desktop run correctly fails readiness by 272 seconds; validation should use the frozen probe/reference window. K18 HRV still failed on the frozen window: gated K18 RMSSD 110.5 ms, SDNN 108.9 ms, mean NN 1048.3 ms versus H6M RMSSD 156.5 ms, SDNN 172.4 ms, mean NN 1032.0 ms. Raw K18 preserved more variability in the same window (RMSSD 175.3 ms, SDNN 154.5 ms) while the 8 bpm HR gate removed many high-variability rows, so the next HRV work is not more catch-up; it is quality-gate/interval classification across repeated captures.
- Learning: A K18 quality-gate sweep across the 2026-06-14 paired captures found a tempting but unsafe diagnostic rule: accept the current 8 bpm HR gate plus locally consistent HR-mismatch frames within a 120-second K18 rhythm band, with no RR below 600 ms and mean HR disagreement <=20 bpm. It improves the latest failing run's aggregate RMSSD error to about 0.3 ms and keeps the earlier clean pass acceptable, but it still fails SDNN, mean NN, and 5-second binned agreement on the latest run. H6M row labeling explains why: in `1781449854`, only 13/99 diagnostically added K18 rows were within 80 ms of the local H6M median and the median added-row error was 176 ms. Do not promote permissive K18 recovery just because aggregate RMSSD improves; require local row/segment agreement and repeat captures.
- Learning: The 2026-06-14 `1781452908` iOS export plus desktop H6M run is another strong coverage run but not an HRV promotion pass. K18 export readiness passed for the 15:43:11Z-15:58:00Z H6M window; latest quality-gated K18 RR sample time was 15:58:01Z, about 3 seconds past the RR-reference target. HRV still failed with the current gate: gated K18 RMSSD 118.3 ms, SDNN 165.8 ms, mean NN 1103.7 ms versus H6M RMSSD 162.4 ms, SDNN 195.3 ms, mean NN 1079.1 ms; 5-second binned RR MAE was 54.8 ms despite correlation 0.884. Local-continuity candidate gates can bring aggregate RMSSD/SDNN/mean close, but binned MAE remains about 43.8 ms and added rows still have high local H6M error. K20 again matched most reference beats but over-detected badly: best sequence matched 808/823 reference beats while emitting 2,065 extras, precision 0.281, RMSSD error 74.1 ms. This reinforces that K18 has the best signal today, but interval classification must satisfy segment-level agreement before promotion.
- Learning: The 2026-06-14 `1781469645` mostly-rest iOS export plus desktop H6M run is the strongest K18 convergence since the first clean pass, but still not a full promotion pass. K18 export readiness passed for the frozen 15-minute target; the full comparison only failed SDNN, with gated K18 RMSSD 36.0 ms, SDNN 71.8 ms, mean NN 699.4 ms versus H6M RMSSD 44.8 ms, SDNN 93.2 ms, mean NN 699.7 ms, 5-second binned RR MAE 19.8 ms, and correlation 0.887. A cleaner target window labeled 20:24Z-20:29Z clipped to actual overlap 20:24:01Z-20:26:56Z and passed all current gates: 259 gated K18 intervals versus 274 H6M intervals, RMSSD error 4.5 ms, SDNN error 0.6 ms, mean NN error 0.6 ms, binned MAE 7.8 ms, and correlation 0.944. User labels and K21 motion summaries agree that early movement, then a still/rest segment, then another posture/location transition split the run; K21 was very quiet around 20:24Z-20:25Z and spiked around 20:27Z-20:28Z. The next K18 work should be a non-oracle rest-window selector with minimum contiguous quality-gated RR coverage, rather than promoting broad active daytime windows.
- Learning: The 2026-06-14 `1781471908` reclined/low-motion iOS export plus desktop H6M run moved the K18 blocker from movement/window quality to quality-gate semantics. The frozen 21:01:17Z-21:16:17Z target passed export readiness, and K21 motion was very quiet from about 21:03Z-21:15Z, but the current warning-free plus per-interval 8 bpm HR-marker gate failed every 5- and 8-minute subwindow. Raw plausible K18 tracked H6M closely: full-window raw K18 RMSSD 138.2 ms, SDNN 137.0 ms, mean NN 891.6 ms versus H6M RMSSD 130.6 ms, SDNN 133.7 ms, mean NN 888.6 ms; minute-level mean RR also matched closely. The hard gate reduced K18 to RMSSD 86.3 ms and SDNN 101.5 ms by dropping many `normal_history_k18_rr_heart_rate_mismatch` rows, so HR-marker mismatch should become a context feature under rest/coverage gates rather than an unconditional rejection. Do not promote raw K18 yet; require a non-oracle gate that preserves true high variability while still rejecting false long/short artifacts and passing binned local agreement.
- Learning: K18 HRV validation now emits `diagnostic_gate_summaries` for `raw_plausible`, `bounded_plausible_560_1300`, and `local_continuity_260ms` without changing the trusted current gate. Cross-run diagnostics show why promotion is still blocked: on `1781471908`, raw/bounded pass the aggregate tolerances (RMSSD error 7.6 ms, SDNN error 2.9-3.3 ms, binned MAE about 19.9 ms, correlation 0.942), but on `1781469645` raw/bounded admit bad active-window intervals and fail badly, while `1781449854` still fails all diagnostic candidates. The next classifier should be a rest/segment selector plus contextual K18 row acceptance, not a broad raw/bounded K18 promotion.
- Learning: K18 HRV validation now emits `diagnostic_rest_segment_summaries` for non-oracle 5-minute rest-window selection using K10/K21 motion plus K18 coverage, then evaluates bounded relaxed K18 intervals inside those windows against H6M. This selector found three clean validation windows only in the low-motion `1781471908` run: 21:07:18Z-21:12:18Z, 21:08:18Z-21:13:18Z, and 21:09:18Z-21:14:18Z, with RMSSD error 0.2-0.7 ms, SDNN error 1.0-1.6 ms, mean NN error 0.5-1.1 ms, binned MAE 13.4-19.9 ms, and correlation 0.936-0.971. The same rest selector also selected some older low-motion-looking windows that failed H6M validation, so motion plus coverage is necessary but not sufficient; the next classifier needs an additional K18-only segment-quality feature before promotion.
- Learning: The first K18-only rest-segment quality discriminator narrows the candidate set but does not finish promotion. It requires the non-oracle rest gate, continuous bounded K18 candidate coverage, enough high-variability signal (`RMSSD >= 120 ms`, `SDNN >= 115 ms`, `pNN50 >= 0.50`), and either full current-gate coverage or a meaningful relaxed-row contribution. Across `1781449854`, `1781469645`, and `1781471908`, it rejects all older low-motion-looking false positives and keeps only `1781471908`; however, it still keeps early `1781471908` windows from 21:03Z-21:11Z that fail H6M binned agreement while later 21:07Z-21:14Z windows pass. Existing K18-only aggregate/trend/candidate-current-bin features do not cleanly separate those two groups, so the next step needs another low-motion or overnight-like paired capture rather than another threshold tweak on the same run.
- Learning: The 2026-06-14 `1781478471` iOS export plus desktop H6M run is a partial repeat, not a full-window validation, because K18 had not caught up when the bundle was created. K18 export readiness failed for the 22:49:57Z-23:07:40Z overlap: latest quality-gated K18 RR sample time was 23:05:15Z, 145 seconds behind the RR-reference target. Clipping validation to the usable 22:49:57Z-23:05:15Z K18 sample window still failed the current strict gate (K18 RMSSD 89.5 ms, SDNN 78.9 ms, mean NN 976.0 ms versus H6M RMSSD 105.0 ms, SDNN 109.4 ms, mean NN 957.6 ms; binned MAE 26.6 ms, correlation 0.847), but diagnostic bounded-plausible K18 stayed close on aggregate (RMSSD 115.1 ms, SDNN 109.2 ms, mean NN 963.1 ms; binned MAE 21.6 ms, correlation 0.919). One non-oracle rest segment, 22:52:57Z-22:57:57Z, passed H6M reference validation with RMSSD error 2.0 ms, SDNN error 4.0 ms, binned MAE 17.6 ms, and correlation 0.963, but the current K18-only quality discriminator rejected it because of a 47-second candidate gap, low relaxed-row contribution, and the fixed `RMSSD >= 120 ms` floor. This is real progress: the next selector should replace fixed high-variability floors with coverage/continuity and local-shape confidence so valid lower-RMSSD rest segments are not rejected.
- Learning: The 2026-06-15 `1781482072` iOS export confirms the automatic probe catch-up tooling now works for the frozen 15-minute target. The app started a `2460` second diagnostic capture fail-safe and kept band capture open from 23:50:15Z to 00:07:42Z, after the RR reference window ended. K18 export readiness passed for the frozen 23:50:21Z-00:05:15Z target: latest quality-gated K18 RR sample time was 00:04:57Z, 17 seconds behind the reference target and inside the 30-second grace. The full H6M session ran about 40 seconds beyond the frozen target and correctly failed readiness by 57 seconds, so validators should keep using the frozen automatic probe window rather than the whole manually stopped H6M session. HRV still failed the current strict gate on the frozen window (K18 RMSSD 65.5 ms, SDNN 75.4 ms, mean NN 891.7 ms versus H6M RMSSD 90.9 ms, SDNN 101.4 ms, mean NN 878.8 ms; binned MAE 25.6 ms, correlation 0.826). Diagnostic `local_continuity_260ms` got aggregate metrics close (RMSSD error 1.3 ms, SDNN error 5.4 ms, mean NN error 7.0 ms) but binned shape still failed (MAE 22.5 ms, correlation 0.787). Another non-oracle rest segment, 23:52:05Z-23:57:05Z, passed H6M reference validation with RMSSD error 6.5 ms, SDNN error 1.9 ms, binned MAE 17.5 ms, and correlation 0.860, yet the current K18-only discriminator rejected it due to fixed low-RMSSD/pNN50 floors and low relaxed-row contribution. This reinforces that the next implementation should replace fixed high-variability floors with continuity/local-shape confidence and segment-level binned-shape predictors.
- Learning: K18 HRV validation now emits diagnostic sliding 5-minute windows with `reference_label`, `k18_only_decision`, `failure_reasons`, and `timebase_audit` fields. The K18-only rest decision no longer treats low RMSSD/SDNN/pNN50 as a quality failure; it uses rest/motion coverage, current/candidate interval coverage, and candidate continuity instead. On `1781482072`, the previously rejected 23:52:05Z-23:57:05Z segment now appears as a true positive (`k18_only_decision=pass`, `reference_label=pass`, RMSSD error 6.5 ms, SDNN error 1.9 ms, binned MAE 17.5 ms, correlation 0.860). The same run also exposes the next blocker: several adjacent quiet windows are false accepts where aggregate RMSSD/SDNN are close but binned correlation/shape fails, so the next classifier feature should be a K18-only predictor for local temporal shape or row dropout, not another fixed variability floor.
- Learning: A first cross-run sliding-window corpus over 11 paired iOS/H6M runs produced 100 five-minute windows: 4 true K18-only passes, 31 true rejects, 17 false accepts, 8 false rejects, and 40 unknowns. This proves the best path is not blocked by K18 mapping, but the current K18-only rest decision is not precise enough for promotion. The next implementation should learn from false accepts/false rejects across the corpus and add non-reference features for temporal-shape trust, row dropout, and maybe posture/motion transition boundaries before any HRV value reaches Recovery.
- Learning: `open-vitals-k18-hrv-corpus-evaluator` now turns saved K18 validation reports into an explicit promotion gate. On the current 11-run corpus it reports 100 windows, 21 K18 pass decisions, 4 true accepts, 17 false accepts, precision 0.190, recall 0.333, and blockers `k18_pass_in_reference_fail_window`, `k18_rejects_reference_pass_window`, and `reference_unknown_windows_present`. Outcome medians show why a simple fixed threshold is risky: false accepts have near-complete candidate coverage (`interval_count_expected_ratio` median 0.989) and low aggregate RMSSD/SDNN errors, while their binned shape still fails (median binned MAE 21.4 ms, correlation 0.848). Keep K18 HRV diagnostic-only until corpus precision improves materially on held-out paired runs.
- Learning: K18 validation now emits runtime-safe segment features for candidate sample coverage/gap distribution, candidate-vs-current binned shape, and pre/edge/post motion context. The corpus evaluator scores simple non-reference rule candidates against H6M labels. None are promotable on the current 11-run corpus: the best simple high-shape rule selects 5 windows with 3 true accepts and 2 false accepts (precision 0.600), while preserving the latest lower-HRV true pass also preserves six nearby false accepts. This means the next improvement needs either a richer row-context classifier or new held-out captures; do not ship a threshold-only K18 HRV selector from the current corpus.
- Learning: Row-context K18 diagnostics now classify relaxed bounded intervals against local current-gated medians. This also does not produce a safe selector: true accepts have median relaxed locally plausible fraction 0.902, short excursions 2, long excursions 11, and local p95 delta 303.8 ms, while false accepts have median plausible fraction 0.916, short excursions 1, long excursions 4, and p95 delta 290.5 ms. Rules that require relaxed rows to be locally plausible select only reference-fail windows. The current corpus has reached the capture-needed point for classifier work: additional held-out, low-motion paired sessions are needed before promoting any K18 HRV rule.
- Learning: The 2026-06-15 `1781488620` export confirms the automatic probe can wait long enough for K18 catch-up on a frozen 15-minute target. K18 export readiness passed for 01:34:42Z-01:49:42Z: latest quality-gated K18 RR sample time was 01:50:14Z, roughly 32 seconds beyond the H6M target. HRV still failed the current strict gate (K18 RMSSD 78.4 ms, SDNN 92.9 ms, mean NN 938.8 ms versus H6M RMSSD 139.2 ms, SDNN 140.8 ms, mean NN 931.5 ms; binned MAE 44.9 ms, correlation 0.723). The bounded-plausible diagnostic gate nearly nailed aggregate RMSSD (139.1 ms versus 139.2 ms) but still missed SDNN by 10.1 ms and had poor binned shape (MAE 41.0 ms, correlation 0.784). Across its sliding windows, K18 produced 6 true rejects and 5 unknowns with no pass windows, mostly due to binned correlation/shape and candidate gaps. This is useful negative evidence: the blocker is not the HRV formula or export wait, it is K18 temporal-shape/dropout classification.
- Learning: After adding `1781488620`, the K18 HRV sliding corpus contains 12 reports and 111 five-minute windows: 4 true accepts, 37 true rejects, 17 false accepts, 8 false rejects, and 45 unknowns. Current K18-only pass precision remains 0.190 and recall remains 0.333. A stricter non-reference temporal-variability candidate (`k18_pass_strict_temporal_variability_combo`) selects 2 true pass windows and 0 false/unknown windows in the corpus, but both true accepts come from the same prior capture, so it is a tracking candidate only and needs held-out repeats before any promotion.
- Learning: The 2026-06-15 `1781490508` export is another valid frozen-window K18 catch-up run but not a pass candidate. Readiness passed for 02:12:31Z-02:27:31Z with latest quality-gated K18 RR sample time at 02:27:18Z, inside the 30-second grace. Current strict K18 HRV failed (RMSSD 59.4 ms, SDNN 59.3 ms, mean NN 888.6 ms versus H6M RMSSD 74.3 ms, SDNN 81.2 ms, mean NN 880.8 ms; binned MAE 24.3 ms, correlation 0.736). Diagnostic `local_continuity_260ms` almost solved aggregate HRV (RMSSD error 3.9 ms, SDNN error 2.2 ms, mean NN error 4.3 ms) but still failed temporal shape (binned MAE 21.3 ms, correlation 0.789). All eight sliding windows were either true rejects or unknowns, and the corpus now has 13 reports, 119 windows, 4 true accepts, 40 true rejects, 17 false accepts, 8 false rejects, and 50 unknowns. The strict temporal-variability candidate remains repeat-required with 2 true accepts and no false/unknown windows.
- Learning: A K18 gap audit on `1781490508` shows the temporal-shape failure is real row dropout, not just an over-strict RR filter. In the frozen 02:12:31Z-02:27:31Z target, usable K18 RR rows start at 02:15:41Z, giving a 190-second initial hole, and the 368 RR-bearing K18 frames contain only 420 intervals. There are six K18 sample-time gaps from 32-98 seconds; each gap's historical page delta equals its device-time delta, which indicates whole K18 history seconds are missing from the decoded/offloaded stream. Treat candidate interval coverage, sample gaps, and page-continuity as first-class K18 segment-quality features before trusting aggregate RMSSD/SDNN parity.
- Learning: Bedtime validation must avoid the normal decoded health-packet capture path. A stopped test run grew to about 9 MB within minutes even after adding a 3-hour cap, so Stream Probe Plan now starts Overnight Guard in lean raw-spool mode: full decoded SQLite packet capture stays off, while raw spool sidecars, range polls, watchdog, and final sync/export remain active. Use the full Capture > Overnight Guard route only for explicit storage-heavy experiments.
- Learning: Bedtime Export needs storage controls at every phase, not just lean capture. Overnight Guard now blocks start/export below a free-space minimum, surfaces free-space and spool-cap status in More, and gracefully stops collection before critical free-space or raw-spool limits threaten the device.
- Learning: The current paired captures do not show a replacement packet that unblocks HRV ahead of K18. K20 remains beat-correlated but over-detects without a runtime-safe one-peak-per-cycle selector; K21/K10 motion can inform quality but not RR timing; K26 is too sparse or still semantically unresolved; K17/R17 and K16 are capture-dependent and not consistently present. The next useful work is more paired iOS/H6M sessions plus a row-context classifier that labels K18 rejected rows as short-split artifacts, locally plausible long intervals, mean-HR mismatches, or unresolved against motion/events/K20/K26 context.
- Learning: A BLE historical sync timestamp is not proof that packet-derived health scores have new local evidence. Automatic historical sync must open or reuse a packet import session before requesting missed packets, then refresh packet inputs/scores after the import session closes; otherwise the UI can truthfully show a recent band sync while Sleep, Recovery, and Strain still load stale or empty persisted score reports.
- Learning: Home's selected score date must drive packet-score loading, recompute windows, and stale-report guards together. It is not enough to change the title to "Yesterday"; if the store still requests today's local-day window or `ScoreDateTimeline` blanks non-today values, valid selected-day packet evidence will render as `--`.
- Learning: Persisted packet-score reports can be partial for a selected local day. A loaded Sleep report must not short-circuit the loader when Strain is missing; with `recomputeIfMissing`, the store should still run the bridge for absent displayable families.
- Learning: Health Monitor packet inputs need the same selected local-day window as Daily Scores. Resting HR can be computable from yesterday's packets while the UI still says `Run pending` if packet input rollups stay pinned to today's empty window.
- Learning: Home Daily Scores should explicitly run the selected-date score pass instead of relying only on a persisted-score load callback. Date switches can otherwise race with an in-flight load and never enqueue the recompute needed for missing Strain.
- Learning: Latest decoded health-packet evidence can belong to the previous local day even when the user opens Home today. Home Daily Scores should align an initial Today selection to the latest local packet-evidence day when Today has no packet evidence, then run packet inputs/scores for that day; otherwise the UI can mix an empty Today strain state with all-time/stale sleep reports and make valid previous-day strain look missing. Prefer decoded historical/realtime packet bounds over raw evidence bounds so later command replies do not pull the score date forward.
- Follow-up: Keep everyday health screens calm and move operational detail into More.

### Active UI Palette Comes From The Logo

- Context: The Home, More, and shared Health SwiftUI surfaces were redesigned around the OpenVitals logo colors.
- Learning: Active app chrome should stay in the logo-derived graphite, charcoal, bronze, gold, champagne, and ivory family; avoid reintroducing red/green/blue/purple/pink metric or status tints on everyday health and operational surfaces.
- Follow-up: Use opacity, stroke style, labels, symbols, and provenance copy to distinguish states before adding new hues.

## Validation

### Command Writes Need Multiple Gates

- Context: The Rust core has command evidence, capture plans, direct-send gates, and critical-command requirements.
- Learning: Direct BLE writes should require validated evidence, visible user intent, dry-run bytes, session logging, connected device state, and explicit critical confirmation when relevant.
- Learning: Stream probes must use the same command gates as direct writes; do not treat optical, Labrador, raw, IMU, research, or persistent stream commands as harmless debug toggles.
- Follow-up: Never expose a new direct command button as a casual tap before the Rust gate and UI confirmation path exist.

### Health Sync Needs Semantic Guards

- Context: The Rust health sync dry-run includes permission, unit, idempotency, provenance, backfill, cleanup, and HRV semantic checks.
- Learning: Platform health writes should be dry-run validated first, and RMSSD HRV should not be written into incompatible HealthKit fields.
- Follow-up: Keep HealthKit integration behind explicit authorization and dry-run/reporting surfaces until the mapping is proven.

### Build And Test Expectations Depend On What Changed

- Context: The README asks contributors to build after touching Swift, Rust bridge, project, or signing settings.
- Learning: Docs-only changes do not require an app build, but Swift/Rust/bridge changes should be verified with Xcode and/or `cargo test` as appropriate.
- Follow-up: In final notes, state which checks ran and which were skipped.

### Xcode GUI Builds Need Rust On Script PATH

- Context: GUI-launched Xcode can run the `Build Rust Core` phase without the user's login-shell `PATH`.
- Learning: `Scripts/build_ios_rust.sh` must make common Rust install locations such as `~/.cargo/bin` visible before invoking Cargo, or Xcode reports `cargo: command not found`.
- Follow-up: If a future phase script invokes developer tools installed outside Xcode, add explicit path discovery and a clear missing-tool error.
